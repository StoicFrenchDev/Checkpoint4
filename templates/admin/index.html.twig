{% extends 'base.html.twig' %}

{% block title %}Cat index{% endblock %}

{% block body %}
    <h1>Cat index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Age</th>
                <th>Breed</th>
                <th>Sex</th>
                <th>Description</th>
                <th>ProfilePicture</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cat in cats %}
            <tr>
                <td>{{ cat.id }}</td>
                <td>{{ cat.name }}</td>
                <td>{{ cat.age }}</td>
                <td>{{ cat.breed }}</td>
                <td>{{ cat.sex }}</td>
                <td>{{ cat.description }}</td>
                <td>{{ cat.profilePicture }}</td>
                <td>
                    <a href="{{ path('cat_show', {'id': cat.id}) }}">show</a>
                    <a href="{{ path('cat_edit', {'id': cat.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('cat_new') }}">Create new</a>
{% endblock %}
