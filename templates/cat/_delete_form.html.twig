<form method="post" class="btn" action="{{ path('cat_delete', {'id': cat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cat.id) }}">
    <button class="btn btn-secondary">Delete</button>
</form>
